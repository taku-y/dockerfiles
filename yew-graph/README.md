```bash
$ sh build.sh
$ sh run.sh

# In container
$ cd ~/my_proj/static
$ npm install plotly.js-dist
$ cd ~/my_ploj
$ sy

# Yew example
$ cd ~/yew/example/counter
$ cargo web start --target=wasm32-unknown-unknown --host=0.0.0.0

# Yew websocket example - client
$ cd ~/yew/example/dashboard
$ cargo web start --target=wasm32-unknown-unknown --host=0.0.0.0

# Yew websocket example - server
$ cd ~/yew/example/server
$ cargo run
```